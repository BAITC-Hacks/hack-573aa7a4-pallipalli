# PalliPalli project instructions

- Repository: `BAITC-Hacks/hack-573aa7a4-pallipalli`.
- Commit and push work to **`galammadin`**. Create that branch if absent. This is the user's standing branch preference.
- Read `docs/PROJECT_CONTEXT.md` for the prior conversation's decisions and current status.
- Preserve the organizer's environment, evaluator, and supplied data files.
- The scored `Agent.act(env)` must remain reproducible, obey all campaign/pilot/resource limits, and use only the public environment interface.
- Keep optional API commentary outside the scored agent. Evaluate proposed policies before integrating them into decisions.
- Read API credentials from the environment. Keep credentials out of code, reports, prompts, and commits.
- Compare algorithm changes against the frozen reference and disclose regressions and local-simulator limitations when evaluation is requested.
